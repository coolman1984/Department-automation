# دليل مشروع الحضور

## نطاق النسخة

هذه النسخة تنفذ مسارًا كاملًا للحضور: رفع ملف ← تحديد رأس الجدول ← تحقق ← دمج آمن ← مؤشرات ولوحات ← تاريخ محلي وتراجع. لا تعالج بعد ملفات الموظفين والورديات والإجازات باعتبارها مصادر مستقلة داخل نفس عملية الرفع.

## عقد البيانات

| البند | القرار الحالي |
| --- | --- |
| الملف | ملف إكسل واحد لكل تحديث |
| الشيت | `TIM_02_DailyAttendance` |
| صف العناوين | ٣ |
| المفتاح | `Attendance_ID` |
| تاريخ الحدث | `Work_Date` |
| التحديث | دمج؛ السجل بنفس رقم الحضور يستبدل نسخته السابقة |
| الحذف | غير مستنتج من غياب السجل |
| الغياب | يحسب فقط من حالة `Absent` الصريحة |
| الملف المتكرر | نفس البايتات لا يعالج مرتين حتى لو تغير الاسم |
| ملف غير صالح | لا يستبدل آخر نتيجة سليمة |

الحقول الحرجة: رقم الحضور، رقم الموظف، تاريخ العمل، وحالة الحضور. الأرقام مثل دقائق التأخير اختيارية؛ القيمة الفارغة فيها لا تتحول إلى رقم صفر بصمت في المصدر، لكن حساب الجمع لا يضيف لها قيمة.

## أين تغير المنطق؟

غيّر `PROJECT.json` أولًا: أسماء الأعمدة، المفاتيح، المؤشرات، الفلاتر والرسوم. استخدم `CUSTOM_RULES.py` فقط لتحويل صف محدد لا يعبر عنه الإعداد. لا تعدل المحرك إلا عندما تكون القدرة المطلوبة غير متاحة في الإعداد أو التحويل، ثم دوّن السبب، شغل الاختبارات، وحدّث قفل المكونات عبر البناء.

## التوسعة التالية

ملفات النموذج التجريبي تفصل الحضور عن الموظفين والورديات والإجازات. لتوسيع النسخة يجب أولًا اتخاذ قرار شكل المدخل: ملف يومي مركب بشيتات ثابتة، أو استيراد ملفات متعددة/مجلد، أو ملف مضغوط. هذا تغيير قدرة في الاستيراد، وليس مجرد إضافة رسم؛ لا يضاف قبل تحديد عملية الرفع الواقعية.

بعد تثبيت المدخل، نربط الحضور بالوردية حسب الموظف وتاريخ العمل، ثم نضيف الإجازات والموظف والاحتياج للعمالة. يجب أن تأتي كل مرحلة بأمثلة قبول مستقلة، ولا نستخدم الصفوف الصناعية العشوائية كدليل أن قواعد أعمال المصنع صحيحة.

## الفحص قبل التسليم

شغّل:

```text
python CHECK_ENVIRONMENT.py
python SMOKE_TEST.py
python TEST_HR_FOUNDATION.py
python BUILD_PROJECT.py
```

اختبار الأساس يثبت قراءة ٢٠٠ سجل صناعي، منع التكرار، تصحيح سجل قديم مع إضافة جديد، وبقاء آخر نتيجة عند ملف معيب. نتائج نظام لينكس لا تثبت وحدها تشغيل `START.bat` أو إكسل المكتبي على جهاز ويندوز؛ يسجل ذلك كتحقق متبقٍ حتى تجربته على جهاز الهدف.
