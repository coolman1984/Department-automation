# تكليف سونيت ٥ — مستوى عالٍ

## المهمة الآن

أنت تستلم مشروعًا حقيقيًا قيد البناء: **نظام حضور موارد بشرية محلي**. الهدف ليس إعادة بناء مشروع جديد؛ الهدف هو توسيع الأساس المُثبت بحذر، مع إبقاء التشغيل اليومي سهلًا لموظف غير تقني.

النسخة الحالية **V1.0** تدعم الحضور فقط. ثبتت الاختبارات أن:

- تقرأ شيت `TIM_02_DailyAttendance` من الصف الثالث.
- تدمج بالمعرّف `Attendance_ID`.
- تمنع إعادة رفع نفس الملف.
- تحفظ التاريخ عند التصحيح.
- لا تسمح لملف معيب أن يفسد آخر نتيجة سليمة.

## اقرأ بهذا الترتيب قبل أي تعديل

1. `START_HERE_AI.md`
2. `PROJECT_GUIDE.md`
3. `.workflow/state.json`
4. `.workflow/tasks.json`
5. `.workflow/HANDOFF_AR.md`
6. `project_memory/PROGRAM_MAP.md`
7. `project_memory/PROJECT_LOG.md`
8. `PROJECT.json`

ثم شغّل، بدون تعديل:

```text
python TEST_HR_FOUNDATION.py
python SMOKE_TEST.py
```

## طريقة العمل الإلزامية

1. ابدأ بالمهمة `BLD-01` فقط: تحسين منخفض المخاطر، مثل وضوح دليل المستخدم أو وصف حالة الاستيراد. لا تمس الاستيراد أو المفاتيح أو التاريخ.
2. شغّل اختبار الأساس بعد المهمة، وسجّل الدليل في `.workflow/evidence/` وحدّث الحالة والتسليم والسجل.
3. **توقف** قبل `INT-01` إذا لم يوجد قرار تشغيل صريح. هذه المهمة تحتاج معرفة شكل وصول الملفات يوميًا: ملف واحد، ملفات منفصلة معًا، أو ملف مضغوط.
4. إذا صدر قرار اعتماد «ملفات منفصلة تُرفع معًا»، نفّذ `INT-01` على مراحل صغيرة: عقد مصدر لكل من الموظف والوردية والإجازة والحضور، ثم اختبارات للملف الناقص، والموظف غير المعروف، والتصحيح المتأخر، وعدم مسح التاريخ.
5. بعد كل مرحلة: اختبر، حدّث ملفات الحالة، وسلّم ملخصًا قصيرًا للموديل التالي. لا تعتمد على سياق المحادثة كذاكرة.

## قواعد لا تُكسر

- لا تغيّر `Attendance_ID` كمفتاح دمج من دون موافقة مسجلة واختبارات بديلة.
- غياب الموظف من ملف تحديث جزئي **ليس حذفًا**.
- الغياب يُحسب من الحالة الصريحة `Absent` فقط.
- لا تعدّل الاختبارات لكي تمرر التنفيذ.
- لا تغيّر `engine.py` أو `calculation_engine.py` أو `PROJECT.json` في مهمة منخفضة المخاطر.
- لا تدّعِ ربط الموظفين أو الورديات أو الإجازات قبل اختباره فعليًا.
- لا تستخدم بيانات حقيقية أو حساسة؛ `inputs/` بيانات صناعية للتجربة فقط.
- عند عطل متكرر: أنشئ أصغر مثال يعيد المشكلة وسجّلها بدل تخمين إصلاح معماري.

## معيار التسليم

التسليم المقبول يحتوي على: تغييرات محدودة ومفهومة، اختبارات ناجحة، دليل نتيجة بتاريخ التنفيذ، تحديث حالة المهام، وتحديث سجل المشروع. لا تعلن اكتمالًا إذا كان قرار شكل الرفع أو تجربة ويندوز أو ملف إكسل محمي ما زال غير مختبر.

## ما هو موجود في هذا الريبو

| المسار | الغرض |
|---|---|
| جذر المشروع | الكود والواجهة وتشغيل ويندوز |
| `.workflow/` | المهام، الحالة، الأدلة، وخطة المرحلة |
| `project_memory/` | خريطة المشروع وسجل القرارات |
| `sample/` | ملفات اختبار الحضور |
| `inputs/` | بيانات موارد بشرية صناعية كاملة وقاموس البيانات |
| `reference/relay-operating-system/` | قوالب إدارة التسليم بين الموديلات |
| `reference/master-plan/` | وثيقة مرجعية قديمة للخطة العامة؛ ليست أمر تنفيذ مباشر |

## أمر تشغيل مقترح

> اقرأ `SONNET_5_HIGH_EXECUTION_BRIEF_AR.md` واتبع ترتيب القراءة. نفّذ `BLD-01` فقط، اختبره، وسجّل كل الأدلة والحالة. لا تبدأ ربط الملفات المتعددة قبل قرار شكل الرفع اليومي. لا تعدّل الاختبارات أو الملفات المحمية لتعلن نجاحًا.
